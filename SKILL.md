---
name: scan-pdf-split
description: >
  扫描版 PDF 分类拆分技能。面向无文本层的扫描件（打分明细、评分对比表、评审记录等），确定每页所属类别（标段/章节/文档类型）后拆分合并为独立 PDF。识别路径：技能自带本地批量 OCR 脚本为默认方案（开箱即用，整页直读、单进程循环、可并行，不占模型 token，无需任何外部 OCR 工具）；可选增强：OCR MCP（仅零星补漏、严禁并发）、PaddleOCR-VL（疑难页），均未配置则回退模型视觉整页读图。支持横竖版混排、单文件多类别、无独立标题的连续页（继承归属+核实）、重复扫描副本去重等复杂场景；内置 memory.md 记忆机制沉淀复用经验。
  当用户需要把一个扫描 PDF 按标段、类别、章节或文档类型拆成多个 PDF 时使用。
  触发场景包括：(1) 扫描件按标段/类别拆分，(2) 混装 PDF 按文档类型分拣，(3) 无文本层 PDF 的页码-类别映射识别，(4) 横竖混排扫描件的批量归类。
---

# 扫描 PDF 分类拆分

把一个无文本层的扫描 PDF 按类别拆分为多个独立 PDF，命名"文件类型-子类别"。识别方式：**固定宽窗 OCR + 空白页整页补**——OCR 耗时正比于文字行数，稠密表格页整页识别要 20–50 秒/页（CPU），而固定窗口"顶部 0–30% × 横向 30–100%"只有几行字、约 2–5 秒/页；窗口免逐文件调参（居中/偏右标题都覆盖），窗口什么都没识别出来的页自动整页补识别。视觉读图同样只读整页，不做区域切割。

## 拆分原则

- **事实拆分**：每个输出页的类别归属必须来自实际识别（OCR 或读图），禁止凭"应该是连续的"猜测。蒸馏源任务中 110 页文件为 11 组 × 10 标段交叉排列，类别页并不连续。
- **先小样后批量**：先用 1–3 页样张确定页面方向和锚点正则，再批量执行。
- **识别与拆分分离**：先产出完整的"页码→类别"映射并做连续性审查，再执行拆分。
- **副本去重**：合并件常把同一文档扫描两份（同版式页面文本高度近似即副本）；拆分每个文档只保留一份，副本页写进 plan.json 的 `"_副本"` 键（不输出但计入页数校验）。注意"每评委一份"的打分明细/评分表版式雷同却**不是**副本——须凭评委姓名/签名/分数等差异字段确认，禁止仅凭文本相似度判重。
- **不确定必问用户**：任何无法确信的识别结果（OCR 未命中、锚点矛盾、疑似续页核实后仍存疑等），必须记录为"不确定项"，将**文件名 + PDF 页码**（必要时附该页渲染图路径）发给用户确认类别。拆分执行前不确定项必须清零，禁止带着猜测拆分。
- **拆分必校验**：每个输出文件抽查首页+末页的类别标识与文件名一致，且各输出页数之和（+ 声明的副本页数）= 源文件页数。此校验曾挽回整批错误（源文件被替换后仍按旧映射拆分），不得省略。
- **源文件只读**：旋转、裁剪只在渲染层进行；输出写到独立目录，不覆盖源文件。

## 环境

- **自带运行环境，开箱即用**：本技能目录内置 `.venv`（已装 `pymupdf` + `rapidocr_onnxruntime`），调用脚本一律用 `<技能目录>/.venv/Scripts/python.exe`，**无需安装或下载任何东西**。若技能目录被整体移动导致 venv 失效，重建：`cd <技能目录> && python -m venv .venv && .venv/Scripts/python -m pip install pymupdf rapidocr_onnxruntime`。
- 辅助脚本：`scripts/pdf_split_tools.py`，子命令 `probe / render / ocr / split / verify`（函数也可直接 import 复用）。拼图用 `insert_image` 实现，**不需要 Pillow**。
- **OCR 工具探测（可选增强，非必需）**：会话工具列表中存在 `mcp__<服务名>__` 前缀的 OCR 工具即视为 MCP 可用，如 `mcp__ocr__ocr_image`（RapidOCR，秒级）和 `mcp__ocr__parse_document`（PaddleOCR-VL 深度解析，慢）。**未配置任何 MCP 时技能功能完整**——批量识别走路径 1 自带脚本，兜底走模型视觉读图，直接跳过路径 2/3，不要为配置 MCP 停下向用户索要环境。

## 第 0 步：读取 memory.md

开始前先读技能目录下的 `memory.md`，其中记录的版式参数、坑点优先适用；与本文件冲突时以最新日期条目为准并提示用户。

## 第 1 步：探查（probe）

- **先建工作区**：在源文件旁建独立工作目录（如 `<源文件名stem>_work/`），探查/识别的中间产物（JSONL、拼图、plan.json）与输出目录一律放里面，避免与其他文件混淆（教训：复用的输出目录里残留旧项目 PDF，`verify *.pdf` 循环把旧文件一起渲染了）。
- 页数、每页尺寸与旋转角、有无文本层（`probe` 子命令逐页输出）。
- **防文件替换**：若会话中对该文件有历史认知（之前读过/拆过），必须重新核对页数、大小、mtime，不一致则全部按新文件重来，禁止沿用旧映射。
- 若有文本层 → 直接按文本分类（正则提取标识），不必走图像流程。

## 第 2 步：样张定标

取第 1 页 + 中间 1 页 + 末页（`--pages 1,55,110` 这种稀疏抽样即可），确定三件事：

1. **方向**：扫描件常为"内容横版+页面竖版"，渲染时用 `--rot` 旋转（仅内存生效）。
   - OCR 路径：对样张按 0/90/180/270 试跑 `ocr` 子命令，哪个方向命中锚点/文本量最大即采用。
   - 视觉路径：渲染样张整页读图判定。
2. **分类维度**：页码归属通常是两维的——**文件类型**（签到表/承诺书/评审表/明细表/汇总表/报告等，见文末词表）× **子类别**（标段/项目）。有些类型全场只有一份（如签到表、监督报告），子类别为空。
3. **锚点规则**：
   - 文件类型：核心词匹配（`ocr` 子命令 `--keywords` 逐页记录命中词），允许名称细微出入；按特异性从高到低匹配（如先"评分汇总"后"汇总"、先"会议签到"后"签到"），拿不准 → 不确定项。
   - 子类别：不一定是"标段N"字样，也可能直接是项目名称、机组名等——据样张确定写法。默认正则 `标段\s*(\d+)\s*[（(]([^）)]+)`，用 `--pattern` 覆盖；尽量双锚点互验（如编号后缀 + 名称）。
   - JSONL 存每页全文，换词表/正则离线重分析即可，无需重新 OCR。

## 第 3 步：批量识别（页码 → 类别映射）

产出映射 `{页码: (文件类型, 子类别)}`，子类别可空（如签到表、监督报告通常全场一份）。

**路径 1（首选）：技能自带批量 OCR 脚本**

- `ocr` 子命令两遍制：第一遍**固定宽窗**（顶部 0–30% × 横向 30–100%，150 dpi）逐页快速识别（实测约 2–5 秒/页）；第二遍整页补识别只针对窗口遍**无文本且类型词未命中**的页（`--no-fallback` 可关闭）。模型单进程只加载一次。
- **锚点非每页都有时（编号类锚点常见如此，如只印在明细表/汇总表上）**：锚点未命中不再自动触发整页补；类型用关键词命中定、子类别离线从 JSONL 全文正则提取即可。窗口会裁掉页面左侧 30%，编号前缀可能被截断（如完整编号只识别出后半截），离线正则用宽松匹配、别锚定前缀。确需某页全文的（如续页核实），单独跑 `--pages <页码> --clip 0.0,0.0,1.0,1.0`。
- 正则提取 + 类型核心词命中写 JSONL（含每页文本与识别遍数 `pass`，换正则/词表重分析无需重新识别）。
- 纯本地进程，可按页码段拆成多个 Bash 进程或派子代理并行（如 1–55、56–110 两段），无 MCP 的种种限制。
- 实测基准（本机 CPU，评审表格扫描件 150 dpi）：窗口识别约 4–8 秒/页；两份文件共 179 页锚点识别 **100%（179/179）且零漏检**（未触发整页补）。整页识别 20–50 秒/页（稠密表格页上百行字），110 页整页约 40–90 分钟——**除非确需全文，不要默认整页**。100 dpi 开始认错字，不要用更低分辨率。
- 仍未命中的页：先看 JSONL 文本判断是版式变体（改正则免费重跑）还是疑难页；疑难页转路径 3，再不行转路径 4。

**路径 2（可选·零星补漏）：OCR MCP `ocr_image`**

- 未配置 MCP 时跳过本路径（零星未命中页直接转路径 4 视觉读图或列不确定项）。
- 仅用于几页以内的补识别或样张试跑。两条硬性约束（实测教训）：
  - **严禁并发调用**：MCP 服务为单进程，并发会直接把服务打挂（Connection closed，进程消失且**不自动恢复**）；只能主会话串行调用。
  - **子代理工具列表没有 `mcp__*`**：MCP 路径无法委派子代理，也就无法并行加速——这正是批量必须走路径 1 的原因。
- 中途挂掉的降级顺序：路径 1 本地脚本（同引擎，结果等价）→ 路径 3 → 路径 4 → 不确定项问用户。注意别台机器可能没有用户自建的本地 OCR 工具，**可移植的兜底是技能自带脚本**，不要依赖外部安装。

**路径 3（可选·疑难页精修）：PaddleOCR-VL `parse_document`**

- 未配置该 MCP 时跳过本路径，直接转路径 4。
- 仅用于路径 1/2 失败的少量页。CPU 下每页数分钟起（本机实测单页可超过 20 分钟），**禁止批量**，逐页串行，开始前告知用户预计耗时。

**路径 4（回退）：模型视觉读图**

- 无 OCR 环境时的默认路径。`render` 子命令整页渲染、每张拼 4 页（红色页码标注防串页），逐张读图记录类别。
- 页数多（>40）时可分组派**并行子代理**读图（视觉路径独有此加速方式），主会话汇总做连续性审查。
- 辨认不清的页记录为不确定项，不用最相近的类别硬归。

**兜底**：OCR 环境（本地脚本与 MCP）不可用且模型亦无识图能力时，明确告知用户"无法自动识别，请配置 OCR 工具或人工分类"，**不得编造映射**。

## 第 4 步：复杂场景处理

1. **横竖混排**：
   - OCR 路径：逐页自动定向——按 0/90/180/270 试跑取锚点命中或文本量最大者，写进 `--rot-map`；先用页面宽高比粗筛候选方向减少试跑。
   - 视觉路径：先跑"方向拼图"——整页缩略图（宽约 250–300 px）每张拼 20–30 页，一次读图判定每页旋转角（缩略图足以判断横竖）。
2. **多类别混装**：不预设类别清单和排列规律；逐页识别后按实际结果聚类。注意类别可能交叉排列（如 11 组 × 10 标段），拆分需支持跨页抽取合并非连续页（`split` 的 plan 页码列表天然支持）。
3. **无标题/表头的连续页**：无锚点的页标记 `unknown` → 暂定继承前一页类别 → 逐页核实确为续页（OCR 路径检索该页全文：无标题锚点且正文与前页延续；视觉路径整页读图确认）。**位于类别交界处的 unknown 页必须逐页确认，不得直接继承跳过**；核实后仍无法确定的，转入不确定项发用户确认。最终映射中不允许存在未核实的猜测页。

**不确定项汇总确认**：执行拆分前，将全部不确定项逐条列给用户——文件名、PDF 页码、识别到的局部内容或疑因；可用 `verify` 子命令渲染该页整页图并把图片路径一并发给用户。用户确认类别后回填映射；全部清零后方可进入第 5 步。

## 第 5 步：拆分与校验

- 拆分（前置条件：不确定项已清零）：按映射用 `split` 输出到工作区下的独立目录（如 `拆分结果/`），文件名 `文件类型-子类别.pdf`（无子类别的类型直接 `文件类型.pdf`）；重复扫描的副本只保留一份，副本页写入 plan.json 的 `"_副本"` 键；源文件不修改。脚本校验 输出页数 + 副本页数 = 源文件页数。
- 校验（必须）：每个输出文件渲染首页+末页（`verify <pdf> 1,-1`），OCR 文本或读图比对类别与文件名一致。
- 向用户输出拆分清单：每个文件的页数与源页范围。

## 第 6 步：记忆沉淀（每次使用后执行）

- 回顾本次使用中产生的新经验：有效的参数、新版式、新坑、省时做法。
- **memory.md 只记录现有文档（SKILL.md、脚本等）中没有的内容**；与文档重复的经验不入 memory。若 memory 条目日后被吸收进 SKILL.md，同步删除该条目，避免重复。
- 用 1–3 条总结告诉用户，**询问是否写入 memory.md**；用户确认后按既有格式追加（带日期）。
- 不擅自改写 SKILL.md；流程级变更先征求用户同意。

## 附：评标文档常见类型词表

包括但不限于此表；实际文档名称可能有细微出入，按核心词匹配、特异性从高到低（如"会议签到"先于"签到"，"评分汇总"先于"汇总"）。同页命中多个类型词或语义含糊的，转不确定项。

| 编号 | 常见全名 | 核心匹配词 |
|---|---|---|
| 1.1 | 招标方签到表 | 签到（注意排除"会议签到"） |
| 1.2 | 招标方会议签到表 | 会议签到 |
| 1.3 | 保密承诺书 | 保密承诺 |
| 1.4 | 廉洁从业承诺书 | 廉洁 |
| 2 | 商务组初步评审表 | 商务 + 初步评审 |
| 3.1 | 技术1组初步评审表 | 技术1组 / 技术一组 |
| 3.2 | 技术2组初步评审表 | 技术2组 / 技术二组 |
| 4 | 初步评审情况汇总表 | 初步评审 + 汇总 |
| 5 | 商务报告附表 | 附表 |
| 6 | 技术评分明细表 | 明细 |
| 7 | 技术评分汇总表 | 评分汇总 |
| 8 | 评标报告签字页 | 签字 |
| 9 | 监督报告 | 监督 |

## 辅助脚本用法

```bash
# PY 指向技能自带解释器，T 指向脚本（按技能实际安装目录替换）
PY="<技能目录>/.venv/Scripts/python.exe"
T="<技能目录>/scripts/pdf_split_tools.py"

# 页码语法（全子命令通用）：1 起，逗号分隔，支持区间 1-10、负数 -1=末页，如 "1,55,110" "3--1"
# 未知参数会直接报错，不会静默忽略

# 探查：页数/尺寸/文本层
"$PY" "$T" probe <pdf>

# 批量 OCR：固定宽窗快速识别，窗口无文本页自动整页补；结果（含每页文本）写 JSONL
"$PY" "$T" ocr <pdf> out.jsonl --rot 270 [--pages 1-110] [--pattern "标段\s*(\d+)"] [--keywords 签到表,承诺书,初步评审,附表,明细,评分汇总,签字,监督] [--no-fallback]

# 渲染整页拼图（输出 <前缀>-01.png ...，红色页码标注），供视觉路径或人工核对
"$PY" "$T" render <pdf> <输出前缀> --rot 270 [--pages 1,55,110] [--per 4]

# 按映射拆分（plan.json: {"输出名.pdf": [页码...]}，可非连续；"_副本": [页码...] 声明重复页，不输出但计入校验）
"$PY" "$T" split <pdf> <输出目录> plan.json

# 校验：渲染指定页拼图（1,-1 = 首页+末页）
"$PY" "$T" verify <pdf> 1,-1 check.png --rot 270
```
